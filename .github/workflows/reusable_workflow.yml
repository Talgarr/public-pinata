name: reusable workflow

on:
  workflow_call:
    
permissions: {}
  
jobs:
  legit:
    runs-on: ubuntu-latest
    steps:
      - run: echo "legit"
      - name: "treue"
        if: true
        run: echo "test"
      
